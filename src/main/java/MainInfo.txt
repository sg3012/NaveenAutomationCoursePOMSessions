--> All classes / source code files that don't have any tests written in them i.e., 
      any @Test annotations in them should be written under src/main/java package.
      Else they should be written under src/test/java.